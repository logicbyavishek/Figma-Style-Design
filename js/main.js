const createBtn = document.querySelector('.top-actions');
createBtn.addEventListener('click', () => {
  console.log('Create button clicked!');
  window.location.href = "./canvas.html"; // Navigate to canvas.html
});